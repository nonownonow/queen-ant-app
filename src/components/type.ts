export enum TYPE {
  native,
  material,
}

export enum MODE {
  list,
  form,
  ui,
  faker,
}

export enum ROUTE_MODE {
  private,
  public,
}

export enum ROUTE_TYPE {
  none,
  mobile,
  desktop,
  all,
}
